# Revisions that changed formatting only, and that `git blame` should look
# through rather than at. Enable them locally with:
#
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file by itself and needs no configuration.

# Apply ruff format
5d954e3e02cbdf0b35d1137998f70a995bc5ce7f
