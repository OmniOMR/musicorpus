{
    "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",

    // Run mypy and ruff from the environment rather than the versions bundled
    // with the extensions, so that the editor reports exactly what
    // `.venv/bin/python -m mypy` and `-m ruff` report on the command line.
    "mypy-type-checker.importStrategy": "fromEnvironment",
    "ruff.importStrategy": "fromEnvironment",

    "python.testing.pytestEnabled": true,

    // The formatter owns the layout of the code and CI checks it, so saving a
    // file should produce what CI expects rather than something to fix later.
    "[python]": {
        "editor.defaultFormatter": "charliermarsh.ruff",
        "editor.formatOnSave": true
    },

    "search.exclude": {
        "**/.venv": true,
        "**/__pycache__": true,
        "**/*.egg-info": true,
        "musescore": true
    }
}
